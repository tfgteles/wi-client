import * as React from 'react';
import { Box } from '@mui/material';

export default function Home() {
    return (
        <Box sx={{ width: '100%', minHeight: '300px', backgroundColor: 'pink' }}>
            Home page under construction
        </Box>
    );
}
