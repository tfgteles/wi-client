import * as React from 'react';
import { Box } from '@mui/material';

export default function Play() {
    return (
        <Box sx={{ width: '100%', minHeight: '300px', backgroundColor: 'blue' }}>
            Play page under construction
        </Box>
    );
}
